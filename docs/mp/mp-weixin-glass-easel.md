---
source: https://gitcode.com/dcloud/docs-common/edit/main/tutorial/mp-weixin-glass-easel.md
---

<!-- GENERATED BY docs-sync
hash: c6217be97990d53979ce18115c2a13b3ac2e9234c495f993499bb717426456b3
-->

# 微信小程序适配 glass-easel

新一代组件框架 `glass-easel` 已经可以在微信小程序中自由使用了。`glass-easel` 组件框架具有更多特性和更优性能，适合用来编写、维护复杂的小程序。从微信小程序基础库版本 `3.8.12` 开始，`glass-easel` 组件框架提供了对 WebView 渲染引擎的支持。

下面介绍在 uni-app 和 uni-app x 项目中启用 `glass-easel` 的配置方式。

## 开启方式

### 全局开启

在 `manifest.json` 的 `mp-weixin` 节点中增加 `componentFramework` 配置：

```json
// manifest.json
{
  "mp-weixin": {
    "appid": "your appid",
    "componentFramework": "glass-easel",
		"glassEaselWebview": true,
  }
}
```

### 页面开启

在 `pages.json` 中的页面配置中增加 `componentFramework` 配置：

```json
// pages.json
{
	"pages": [
		{
			"path": "pages/index/index",
			"style": {
				"navigationBarTitleText": "首页",
				"componentFramework": "glass-easel",
				"glassEaselWebview": true
			}
		}
	]
}
```

## 验证步骤

1. 升级到支持 `glass-easel` 的微信开发者工具和基础库版本。
2. 在 `manifest.json` 中添加 `componentFramework: "glass-easel"`。
3. 重新编译到微信小程序。
4. 打开编译产物目录，确认 `app.json` 中存在 `componentFramework` 配置。
5. 使用微信开发者工具运行项目，检查控制台是否存在组件框架相关报错或告警。
6. 在真机上回归主要页面，重点验证页面跳转、组件渲染、事件触发、数据更新和第三方组件。

## 相关文档

- [微信小程序官方文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
